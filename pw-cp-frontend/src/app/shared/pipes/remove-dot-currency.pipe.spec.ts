import { RemoveDotCurrencyPipe } from './remove-dot-currency.pipe';

describe('RemoveDotCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDotCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
